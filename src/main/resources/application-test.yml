spring:
  datasource:
    # Overridden dynamically by PostgresContainerSupport via @DynamicPropertySource
    # Fallback for unit tests that don't extend PostgresContainerSupport (no DB needed)
    url: jdbc:postgresql://localhost:5432/indicadoresar_test
    username: indicadoresar
    password: indicadoresar
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  flyway:
    enabled: true
  batch:
    job:
      enabled: false
